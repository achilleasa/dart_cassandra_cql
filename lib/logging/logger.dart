part of dart_cassandra_cql.logger;

// Define hierarchical loggers
final Logger poolLogger = new Logger("dart_cassandra_cql.ConnectionPool");
final Logger connectionLogger = new Logger("dart_cassandra_cql.Connection");
